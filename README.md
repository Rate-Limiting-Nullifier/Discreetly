# Dev

`npm i`

## Run Redis

Need to have a redis instance running

## Run the backend

`cd packages/server`
`npm i`
`npm run dev`

## Run the frontend

`cd packages/frontend-svelte`
`npm i`
`npm run dev`
